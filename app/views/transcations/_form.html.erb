<%= form_for(transcation) do |f| %>
  <% if transcation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transcation.errors.count, "error") %> prohibited this transcation from being saved:</h2>

      <ul>
      <% transcation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :transcation_type %>
    <%= f.text_field :transcation_type %>
  </div>

  <div class="field">
    <%= f.label :transaction_id %>
    <%= f.number_field :transaction_id %>
  </div>

  <div class="field">
    <%= f.label :transcation_date %>
    <%= f.date_select :transcation_date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
